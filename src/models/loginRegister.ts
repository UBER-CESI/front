export interface loginType {
    email: string;
    password: string;
}

export interface registerType {
    nickname: string;
    email: string;
    password: string;
    typeUser: string;
    firstName: string;
    lastName: string;
    phone: string;
}
